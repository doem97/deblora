Output (as well as intermediate results, logs, etc.) will be automatically saved in `./output` folder.

Please output the extracted features (for linear probing) in `./output/features` folder. We also provided the ready-to-use download script in `./output/features/hf_features.sh` - you could directly download the features from [here](https://huggingface.co/datasets/doem1997/deblora).